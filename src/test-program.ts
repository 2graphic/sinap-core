/// <reference path="../typings/index.d.ts" />
import { expect } from "chai";

describe("unit testing", () => {
    it("tests?", () => {
        expect(1).to.equal(1);

        // go test model serialization and whatnot
    });
});